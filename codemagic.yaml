workflows:
  web-build:
    name: Flutter Web Build
    max_build_duration: 30
    instance_type: mac_mini_m1
    working_directory: life_agent_flutter
    environment:
      flutter: stable
      groups:
        - environment_variables # Codemagic secret group for API_BASE_URL, SUPABASE_URL, etc.
    scripts:
      - name: Get Flutter packages
        script: |
          flutter packages pub get
      - name: Build Web
        script: |
          flutter build web --release \
            --dart-define=API_BASE_URL=${API_BASE_URL:-https://your-render-app.onrender.com} \
            --dart-define=SUPABASE_URL=${SUPABASE_URL:-https://evczsuziidwzlxjokkeb.supabase.co} \
            --dart-define=SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
    artifacts:
      - build/web/**
    publishing:
      codemagic_static_pages:
        subdomain: flowo-app # Optional Codemagic static hosting subdomain

  android-build:
    name: Flutter Android APK Build
    max_build_duration: 30
    instance_type: mac_mini_m1
    working_directory: life_agent_flutter
    environment:
      flutter: stable
      groups:
        - environment_variables
    scripts:
      - name: Get Flutter packages
        script: |
          flutter packages pub get
      - name: Build Android Release APK
        script: |
          flutter build apk --release \
            --dart-define=API_BASE_URL=${API_BASE_URL:-https://your-render-app.onrender.com} \
            --dart-define=SUPABASE_URL=${SUPABASE_URL:-https://evczsuziidwzlxjokkeb.supabase.co} \
            --dart-define=SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
